<!DOCTYPE html>
<html th:replace="~{layout :: layout (~{::title}, ~{::main}, _, ~{::scripts})}">

<head>
	<title>Dentistas</title>
</head>

<body>
	<main>
		<div class="row">
			<div class="col-sm-12">
				<div class="card">
					<div class="card-body table-responsive p-0">
						<table class="table table-hover text-nowrap">
							<thead>
								<tr>
									<th>ID</th>
									<th>Data</th>
									<th>Paciente</th>
									<th>Dentista</th>
									<th>Ações</th>
								</tr>
							</thead>
							<tbody>
								<tr th:each="dentista : ${dentistas}">
									<td th:text="${consulta.id}">183</td>
									<td th:text="${projeto.dataConsulta == null ? '-' : #temporals.format(projeto.dataFim)}">
										01/01/2020</td>
									<td th:text="${consulta.paciente.nome}">Clara Giovana Daniela Duarte</td>
									<td th:text="${consulta.paciente.sobrenome}">Augusto Cesar</td>


									<td>
										<div class="btn-group">
											<a type="button" data-toggle="popover" data-placement="top"
												data-content="Detalhes" data-trigger="hover" class="btn btn-primary"
												th:href="@{/consultas/{id} (id=${consulta.id})}">
												<i class="fas fa-eye"></i>
											</a>
											<a type="button" data-toggle="popover" data-placement="top"
												data-content="Editar" data-trigger="hover" class="btn btn-warning"
												th:href="@{/consultas/{id}/editar (id=${consulta.id})}">
												<i class="fas fa-edit"></i>
											</a>
											<a type="button" data-toggle="popover" data-placement="top"
												data-content="Remover" data-trigger="hover" class="btn btn-danger"
												th:href="@{/consultas/{id}/excluir (id=${consulta.id})}"
												onclick="return confirm('Tem certeza que deseja excluir o dentista?');">
												<i class="fas fa-trash"></i>
											</a>
										</div>
									<td>
								</tr>
							</tbody>
						</table>
					</div>
					<!-- /.card-body -->
				</div>
			</div>
		</div>
	</main>

	<th:block th:fragment="scripts">
		<script>
			$(function () {
				$('[data-toggle="popover"]').popover()
			})
		</script>
	</th:block>
</body>

</html>